"use strict"

/// Declare an object to store the role based effort for a deliverable or project
function RoleEffortModel() {
    // Store the MasterRoleID
    this.MasterRoleID;
    // Store the Master Role name
    this.Name;
    // Store the effort for the current record
    this.Effort;
    // Store the cost for the current record
    this.Cost;
}